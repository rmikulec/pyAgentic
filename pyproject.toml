[project]
name = "objectiveagents"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "colorlog>=6.9.0",
    "ipykernel>=6.29.5",
    "openai>=1.93.2",
]

[dependency-groups]
dev = [
    "black>=25.1.0",
    "deepdiff>=8.5.0",
    "flake8>=7.3.0",
    "pytest>=8.4.1",
]

[tool.semantic_release]
version_source = "tag"
upload_to_pypi = true